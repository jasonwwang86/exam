## 1. `exam-service` 题库数据模型与 API 契约

- [x] 1.1 在 `exam-service` 先编写失败测试，覆盖题目查询、详情、创建、更新、删除、题型增删改查、答案配置校验、权限拒绝与日志行为
- [x] 1.2 新增题型与题目实体、DTO、MyBatis-Plus Mapper/XML 以及 MySQL/H2 schema 数据，包含唯一约束、逻辑删除字段和题型引用关系
- [x] 1.3 实现 `/api/admin/question-types` 的列表、新增、编辑、删除接口，并接入参数校验、引用保护与权限注解
- [x] 1.4 实现 `/api/admin/questions` 的分页查询、详情、新增、编辑、删除接口，并完成题型存在性、难度、分值和答案配置模式校验
- [x] 1.5 补充题库管理菜单/API 权限种子数据，并为题目录入、编辑、删除和题型维护增加 TraceNo 关联业务日志与脱敏校验

## 2. `exam-web` 题库管理流程

- [x] 2.1 在 `exam-web` 先编写失败测试，覆盖题库菜单/路由可见性、查询筛选、题目表单校验、删除确认、题型管理与无权限态
- [x] 2.2 在 `exam-web/src/modules/question-bank/` 下新增类型、服务、页面与组件骨架，使前端 DTO 与后端 REST 契约保持一致
- [x] 2.3 实现题目列表页、关键字/题型/难度筛选、新增/编辑表单、删除确认以及统一加载/空态/错误态展示
- [x] 2.4 实现题型管理交互，并将题型、难度、分值和答案配置联动接入题目表单
- [x] 2.5 将题库菜单与路由集成到现有管理端布局，补充必要样式与交互验证，并确认本模块未扩展到试卷管理、考试计划、题目分类或批量导入导出

## 3. 联调与验证

- [x] 3.1 运行并通过 `exam-service` 的测试集，重点验证答案配置校验、题型引用保护、权限拒绝、TraceNo 透传和日志脱敏
- [x] 3.2 运行并通过 `exam-web` 的 Vitest 用例，并针对题目录入、编辑、删除、查询、题型管理和无权限场景做针对性验证
- [x] 3.3 完成 `exam-web` 与 `exam-service` 的接口联调，确认查询条件、响应字段、错误提示与范围边界一致，且实现结果未扩展到试卷管理或考试计划
