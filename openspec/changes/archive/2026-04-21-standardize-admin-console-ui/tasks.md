## 1. `exam-web` 统一管理台壳层

- [x] 1.1 在 `exam-web` 中接入 Ant Design 的主题配置与基础样式重置，明确管理端统一颜色、间距、圆角和内容容器规则。
- [x] 1.2 重构 `exam-web/src/shared/layouts/AdminLayout`，统一顶部信息区、侧边导航区、主内容承载区和当前菜单高亮逻辑，同时保留现有鉴权与路由行为。
- [x] 1.3 对菜单高亮、受保护路由承载和无可见菜单提示等前端行为补充或调整测试，先让相关断言失败再完成行为实现；纯视觉样式通过人工验收和构建验证。

## 2. `exam-web` 标准化页面模式

- [x] 2.1 在 `exam-web/src/shared` 沉淀管理首页和列表页可复用的页面标题区、筛选区、操作区和数据内容区结构。
- [x] 2.2 将管理首页迁移到统一页面模式，调整标题、摘要卡片和响应式间距，使其与统一壳层风格一致。
- [x] 2.3 将考生管理页面迁移到统一列表页模式，复用共享查询区、操作区和数据区结构，并保持现有 REST 调用、权限控制和导入导出行为不变。
- [x] 2.4 将管理员登录页迁移到与主界面一致的主题语言，统一品牌抬头、表单控件和按钮视觉，同时保持现有登录交互与错误提示行为不变。
- [x] 2.5 收敛登录页和模块页的首屏结构：登录页改为居中单卡片，管理首页与考生管理移除介绍型抬头，仅保留必要标题和内容区。
- [x] 2.6 将考生管理的新增、编辑、导入切换为弹框交互，并通过顶部 `新增` 下拉入口聚合“新增考生”和“导入文件”动作。

## 3. 验证

- [x] 3.1 更新 `exam-web` 相关页面测试，覆盖统一壳层渲染、模块入口显示、考生管理关键区域展示和权限场景。
- [x] 3.2 更新登录页相关验证，确认未登录入口在桌面端和窄屏场景下与主界面主题一致，且原有可访问性与登录提交流程保持可用。
- [x] 3.3 运行 `cd exam-web && npm test` 与 `cd exam-web && npm run build`，并对登录页、管理首页、考生管理页做桌面端和窄屏场景的样式验收。
- [x] 3.4 补充考生管理弹框与 `新增` 聚合入口的交互验证，确认新增、编辑、导入流程和原有导出、状态切换能力可继续使用。
