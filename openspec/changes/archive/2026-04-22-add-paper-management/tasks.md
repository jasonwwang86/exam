## 1. `exam-service` 试卷数据模型与 API 契约

- [x] 1.1 在 `exam-service` 先编写失败测试，覆盖试卷分页查询、详情、创建、更新、删除、题目明细新增/更新/删除、总分汇总、重复加题拒绝、权限拒绝与日志行为
- [x] 1.2 新增试卷与试卷题目明细实体、DTO、MyBatis-Plus Mapper/XML 以及 MySQL/H2 schema 数据，包含唯一约束、逻辑删除字段、题目快照字段和排序字段
- [x] 1.3 实现 `/api/admin/papers` 的分页查询、详情、新增、编辑、删除接口，并完成试卷名称、时长、备注等基础信息校验
- [x] 1.4 实现 `/api/admin/papers/{paperId}/questions` 与 `/api/admin/papers/{paperId}/questions/{paperQuestionId}` 的新增、查询、编辑、删除能力，并完成题库题目存在性、重复加题、题目分值和排序校验
- [x] 1.5 为试卷创建、编辑、删除、加题、调分、移除和排序补充菜单/API 权限种子数据、TraceNo 关联业务日志与日志脱敏校验，并确认本模块未扩展到考试计划、在线答题或自动组卷

## 2. `exam-web` 试卷管理与手工组卷流程

- [x] 2.1 在 `exam-web` 先编写失败测试，覆盖试卷菜单/路由可见性、列表查询、基础信息表单校验、手工组选题、明细调分、删除确认、总分展示与无权限态
- [x] 2.2 在 `exam-web/src/modules/paper-management/` 下新增类型、服务、页面与组件骨架，使前端 DTO 与后端 REST 契约保持一致，并与题库查询结果做组选题适配
- [x] 2.3 实现试卷列表页、关键字查询、新增/编辑基础信息表单、删除确认以及统一加载/空态/错误态展示
- [x] 2.4 实现题目明细维护交互，包括候选题查询、手工加题、题目分值调整、顺序调整、移除题目与总分同步展示
- [x] 2.5 将试卷管理菜单与路由集成到现有统一管理端布局，补充必要样式与交互验证，并确认实现结果未扩展到考试计划、在线答题或自动组卷

## 3. 联调与验证

- [x] 3.1 运行并通过 `exam-service` 的测试集，重点验证空试卷保存、总分自动汇总、重复加题拒绝、权限拒绝、TraceNo 透传和日志脱敏
- [x] 3.2 运行并通过 `exam-web` 的 Vitest 用例，并针对试卷创建、编辑、删除、手工组选题、调分、移除题目和无权限场景做针对性验证
- [x] 3.3 完成 `exam-web` 与 `exam-service` 的接口联调，确认试卷列表字段、详情字段、题目快照、总分/时长展示、错误提示与范围边界一致，且实现结果未扩展到题库管理、考试计划或在线答题
