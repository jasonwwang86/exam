## 1. `exam-service` 基础认证与数据模型准备

- [x] 1.1 在 `exam-service` 中建立管理端认证鉴权所需的基础工程结构，并按 `cn.jack.exam` 包约定创建 `controller`、`service`、`dto`、`entity`、`mapper`、`config`、`common`、`exception` 下的认证相关目录与骨架
- [x] 1.2 设计并落地管理员登录与权限所需的 MySQL 表结构，包括 `admin_user`、`admin_role`、`admin_permission`、`admin_user_role`、`admin_role_permission`、`admin_session`
- [x] 1.3 为基础认证能力实现实体类、DTO、MyBatis-Plus Mapper 与关联查询，确保 REST 响应不直接暴露持久化实体
- [x] 1.4 提供默认管理员、基础角色与基础权限的初始化数据方案，且明确本模块不引入考生管理、题库管理或大屏相关数据

## 2. `exam-service` 认证接口与权限控制实现

- [x] 2.1 实现 `POST /api/admin/auth/login` 的请求校验、密码校验、Token 生成、会话持久化与统一响应 DTO
- [x] 2.2 实现 `GET /api/admin/auth/me`，返回当前管理员基础信息、角色、权限码和基础菜单数据
- [x] 2.3 实现 `POST /api/admin/auth/logout`，使当前 Token 对应会话失效
- [x] 2.4 实现管理端认证拦截、当前用户上下文解析、白名单接口放行与未登录错误处理
- [x] 2.5 实现受保护管理接口的权限校验机制，支持菜单权限或接口权限控制，且不扩展到细粒度数据权限

## 3. `exam-web` 登录页与基础鉴权能力实现

- [x] 3.1 在 `exam-web` 中补齐管理端基础应用骨架（如当前不存在），并接入认证相关 API 服务、请求拦截与类型定义
- [x] 3.2 实现管理端登录页、登录表单校验、登录成功后的 Token 持久化与失败提示
- [x] 3.3 实现应用启动后的 `/api/admin/auth/me` 鉴权初始化、登录态恢复与退出流程
- [x] 3.4 实现受保护路由守卫，确保未登录用户无法进入管理端受保护页面
- [x] 3.5 实现基础菜单或路由权限过滤，以及已登录无权限场景下的无权限页面展示
- [x] 3.6 确保本模块前端范围仅覆盖登录与基础权限控制，不引入考生管理、题库管理或大屏页面

## 4. 联调、验证与范围确认

- [x] 4.1 联调验证登录成功、登录失败、禁用账号拒绝登录、Token 恢复、退出失效等主流程
- [x] 4.2 验证未登录访问受保护路由会跳转登录页，已登录但无权限时菜单不可见且接口返回禁止访问
- [x] 4.3 校验 `exam-web` 与 `exam-service` 的 DTO、权限码和菜单标识保持一致
- [x] 4.4 补充与本模块对应的前端、后端或接口验证步骤，并确认实现结果未扩展到考生管理、题库管理、大屏可视化
