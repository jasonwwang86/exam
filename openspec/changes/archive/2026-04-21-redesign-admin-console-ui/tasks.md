## 1. 变更建模与设计定稿

- [x] 1.1 基于 `Direction C - Enterprise Exam Platform` 校准 `exam-web` 的页面目标，并确认本次变更只影响前端展示层
- [x] 1.2 将新的统一壳层、首页工作台结构和列表页三段式结构写入 `admin-console-ui` 的 spec delta、proposal 和 design 文档
- [x] 1.3 明确行为冻结清单作为实现约束，确保登录、权限、路由和考生管理接口调用行为保持不变

## 2. `exam-web` 共享壳层与基础样式

- [x] 2.1 重构 `exam-web/src/shared/styles/global.css`，建立适配 `Direction C` 的浅色扁平视觉 token、背景和基础边界规则
- [x] 2.2 重构 `exam-web/src/shared/layouts/AdminLayout.tsx` 与对应样式，落地左侧导航、顶部工作区头部和白底主内容结构
- [x] 2.3 调整统一壳层相关测试，先覆盖菜单高亮、工作区承载和无菜单提示等前端行为，再完成实现

## 3. `exam-web` 统一页面骨架

- [x] 3.1 在 `exam-web/src/shared/components` 中重构通用页面骨架，沉淀首页工作台与列表页三段式结构的共享承载方式
- [x] 3.2 确保共享页面骨架可同时支持首页概览区、快捷入口区、图表区以及列表页的筛选区、工具栏和数据区
- [x] 3.3 对共享骨架中的前端行为逻辑进行必要验证；纯样式部分通过人工验收和构建校验确认

## 4. 关键页面迁移

- [x] 4.1 重构 `exam-web` 登录页，使其与新后台主题保持一致，但继续保持聚焦式登录入口与现有登录行为
- [x] 4.2 重构管理首页，使其落地个人信息、统计概览、常用功能和趋势图表的工作台布局
- [x] 4.3 重构考生管理页，使其采用筛选区、工具栏和表格区的三段式结构，同时保留现有新增、编辑、删除、状态切换、导入和导出能力

## 5. 验证与验收

- [x] 5.1 更新 `exam-web` 相关测试，确认默认授权页跳转、页面权限控制、菜单高亮和考生管理关键交互行为保持稳定
- [x] 5.2 运行 `cd exam-web && npm test` 验证前端行为回归
- [x] 5.3 运行 `cd exam-web && npm run build` 验证构建和 TypeScript 编译结果
- [ ] 5.4 对登录页、管理首页、考生管理页执行桌面端与窄屏人工验收，确认其视觉风格、层级和业务可读性符合 `Direction C`
