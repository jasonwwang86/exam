## 1. `exam-service` data model and API contract

- [x] 1.1 在 `exam-service` 先编写失败测试，覆盖考生分页查询、创建、编辑、删除、状态更新以及导入导出控制器/服务行为
- [x] 1.2 新增考生实体、DTO、MyBatis-Plus Mapper/XML 与 MySQL/H2 schema 数据，包含唯一约束、状态字段和软删除字段
- [x] 1.3 实现 `/api/admin/examinees` 的查询、新增、编辑、删除、状态更新 REST 接口，并接入参数校验与权限注解
- [x] 1.4 引入并实现 Excel 批量导入导出能力，返回逐行校验结果、成功失败统计与稳定列定义
- [x] 1.5 补充考生管理菜单/API 权限种子数据，并为创建、编辑、删除、状态、导入、导出增加 TraceNo 关联业务日志和脱敏校验

## 2. `exam-web` management workflow

- [x] 2.1 在 `exam-web` 先编写失败测试，覆盖考生路由可见性、查询筛选、表单校验、状态切换以及导入导出交互
- [x] 2.2 在 `exam-web/src/modules/examinees/` 下新增类型、服务与页面骨架，使前端 DTO 与后端 REST 契约保持一致
- [x] 2.3 实现考生列表页、分页筛选、新增/编辑表单、删除确认和状态切换流程，并接入模块样式与加载/错误反馈
- [x] 2.4 实现导入上传、结果提示、导出下载与无权限态处理，避免把模块业务逻辑堆叠到 `App.tsx`
- [x] 2.5 将考生菜单与路由集成到现有管理端布局，确保 `shared/` 只承载通用布局与跨模块复用能力

## 3. 联调与验证

- [x] 3.1 运行并通过 `exam-service` 的测试集，重点验证唯一约束、软删除、导入导出、权限拒绝、TraceNo 透传和日志脱敏
- [x] 3.2 运行并通过 `exam-web` 的 Vitest 用例，并针对 CRUD、查询、状态切换、导入导出和受限管理员场景做针对性验证
- [x] 3.3 完成 `exam-web` 与 `exam-service` 的接口联调，确认查询条件、响应字段、错误提示、导出文件结构和导入失败反馈一致
