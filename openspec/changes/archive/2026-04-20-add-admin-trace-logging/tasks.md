## 1. `exam-web` TraceNo 透传能力

- [x] 1.1 在 `exam-web` 管理端 Axios 客户端中增加 `TraceNo` 请求头生成与透传逻辑，确保登录、获取当前用户、退出等基础接口都会携带 32 位无连字符 UUID 格式的 TraceNo
- [x] 1.2 明确 `exam-web` 本次前端改动仅负责 TraceNo 透传，不改动现有业务流程与页面范围

## 2. `exam-service` TraceNo 与统一日志实现

- [x] 2.1 在 `exam-service` 中增加统一的 TraceNo 上下文处理能力，支持从请求头读取、非法时替换、缺失自动生成 32 位无连字符 UUID、写入 MDC 并回写响应头
- [x] 2.2 在 `exam-service` 中增加统一请求开始、请求完成与异常日志，确保日志包含 TraceNo、方法、路径、状态码、耗时和可控报文摘要
- [x] 2.3 在 `exam-service` 中实现日志脱敏与输出约束，禁止输出明文密码、完整 Token、完整 Authorization 头和密码摘要
- [x] 2.4 在 `exam-service` 当前已有认证与权限主链路中补充关键逻辑日志，至少覆盖登录成功、登录失败、禁用账号拒绝、鉴权失败、权限校验失败和退出成功，并形成后续模块可复用的通用规范
- [x] 2.5 保持本次后端实现为通用日志基础能力，不修改现有 REST 业务语义，不新增数据库表结构

## 3. 验证与回归

- [x] 3.1 为 `exam-service` 补充测试，验证请求携带 TraceNo 时后端日志与响应头使用相同值，缺失 TraceNo 时会自动生成
- [x] 3.1 为 `exam-service` 补充测试，验证请求携带合法 TraceNo 时后端日志与响应头使用相同值，缺失或非法 TraceNo 时会自动生成合法值
- [x] 3.2 为 `exam-service` 补充测试，验证请求/响应日志核心字段齐全且敏感字段被脱敏
- [x] 3.3 为 `exam-service` 补充测试，验证登录失败、权限拒绝等关键节点会输出可通过 TraceNo 检索的业务日志
- [x] 3.4 为 `exam-web` 补充测试或验证步骤，确认管理端请求会附带 32 位无连字符 UUID 格式的 TraceNo 请求头
- [x] 3.5 完成联调或自动化验证，确认本次实现已形成通用日志基础能力，且未引入外部链路追踪平台接入
