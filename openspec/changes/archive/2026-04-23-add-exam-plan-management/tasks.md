## 1. `exam-service` 考试计划数据模型与 API 契约

- [x] 1.1 在 `exam-service` 先编写失败测试，覆盖考试计划分页查询、详情、创建、更新、考生范围覆盖、状态流转、发布时间校验、权限拒绝与日志行为
- [x] 1.2 新增考试计划与考试计划考生范围关联实体、DTO、MyBatis-Plus Mapper/XML 以及 MySQL/H2 schema 数据，包含状态字段、时间字段、逻辑删除字段、范围唯一约束和菜单/API 权限种子数据
- [x] 1.3 实现 `/api/admin/exam-plans` 与 `/api/admin/exam-plans/{id}` 的分页查询、详情、新增、编辑能力，并完成试卷存在性、开始/结束时间和试卷时长兼容性校验
- [x] 1.4 实现 `/api/admin/exam-plans/{id}/examinees` 的范围覆盖与范围查询能力，以及 `/api/admin/exam-plans/{id}/status` 的状态更新能力，并完成无效考生过滤、空范围发布拒绝和终态限制
- [x] 1.5 为考试计划创建、编辑、考生范围覆盖和状态变更补充 TraceNo 关联业务日志与脱敏校验，并确认实现结果未扩展到考生端登录、在线答题或大屏模块

## 2. `exam-web` 考试计划管理与配置流程

- [x] 2.1 在 `exam-web` 先编写失败测试，覆盖考试计划菜单/路由可见性、列表查询、配置流程表单校验、考生范围选择、状态切换、详情展示与无权限态
- [x] 2.2 在 `exam-web/src/modules/exam-plan-management/` 下新增类型、服务、页面与组件骨架，使前端 DTO 与后端 REST 契约保持一致，并适配既有试卷/考生查询能力作为选择数据源
- [x] 2.3 实现考试计划列表页、关键字与状态筛选、详情查看、新增/编辑配置流程，以及统一的加载态、空态和错误态展示
- [x] 2.4 实现考试时间配置、试卷选择、考生范围批量选择与已选范围预览、状态切换和失败提示交互，确保配置不完整时前后端都能阻止发布
- [x] 2.5 将考试计划菜单与路由集成到现有统一管理端布局，补充必要样式与非 TDD 的针对性页面验证，并确认实现结果未扩展到考生端流程或监考大屏

## 3. 联调与验证

- [x] 3.1 运行并通过 `exam-service` 的测试集，重点验证时间窗口校验、试卷时长兼容性、空考生范围禁止发布、终态限制、权限拒绝、TraceNo 透传和日志脱敏
- [x] 3.2 运行并通过 `exam-web` 的 Vitest 用例，并针对计划创建、编辑、范围设置、状态切换、详情查看和无权限场景做针对性验证
- [x] 3.3 完成 `exam-web` 与 `exam-service` 的接口联调，确认考试计划字段、试卷关联摘要、考生范围数据、状态语义和错误提示一致，且实现结果未扩展到试卷管理、考生端登录或大屏模块
